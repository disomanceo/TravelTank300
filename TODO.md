# TravelTank300 TODO — Step 29

ฐานอ้างอิง UI: Commit `2e1a31d`

## งานที่ทำใน Step 29

- [x] แก้หน้าแรกให้ใช้ `cover_drive_file_id` เป็นแหล่งรูปหลัก
- [x] ใช้ `cover_image_url` เป็น fallback สำหรับข้อมูลเก่า
- [x] โหลดรายการสถานที่ใหม่เมื่อกลับมาที่หน้า `/places`
- [x] โหลดใหม่เมื่อ Browser focus, `pageshow` หรือกลับจาก PWA background
- [x] ตรวจ Error ทุกขั้นตอนของการตั้งรูปหน้าปก
- [x] ตรวจว่าตาราง `travel_places` ถูกอัปเดตจริง ไม่ใช่ RLS ปฏิเสธแบบเงียบ
- [x] รองรับฐานข้อมูลที่มีหรือไม่มีคอลัมน์ `updated_at`
- [x] คง UI จาก Commit `2e1a31d`
- [x] ZIP อยู่ระดับราก ไม่มีโฟลเดอร์โปรเจกต์ซ้อน

## สาเหตุเดิม

หน้าแรกใช้เฉพาะ `cover_image_url` และเก็บ state รายการเดิมไว้เมื่อย้อนกลับจากหน้ารายละเอียด ทำให้แม้เลือกหน้าปกสำเร็จ หน้าแรกอาจยังแสดงภาพเดิมหรือ Placeholder จนกว่าจะ Reload หน้าใหม่

## ต้องทดสอบ

- [ ] `npm.cmd run lint`
- [ ] `npm.cmd run build`
- [ ] เลือกรูปหนึ่งเป็นหน้าปกในหน้ารายละเอียด
- [ ] ตรวจ Badge `รูปหน้าปก`
- [ ] กดย้อนกลับหน้า `/places` แล้วเห็นหน้าปกใหม่ทันที
- [ ] ปิดและเปิด PWA ใหม่แล้วรูปหน้าปกยังถูกต้อง
- [ ] Refresh หน้า `/places` แล้วรูปยังแสดง
- [ ] ทดสอบข้อมูลรูปเก่าที่ไม่มี `cover_drive_file_id`

## ข้อควรระวัง

- หากขึ้นข้อความว่า `ฐานข้อมูลไม่อนุญาตให้อัปเดตรูปหน้าปกของสถานที่` ต้องตรวจ Supabase RLS Update Policy ของตาราง `travel_places`
- ยังไม่ Commit/Push จนกว่า lint, build และการทดสอบจริงจะผ่าน


## Step 29.1 — Build guard for place name
- [x] แก้ TypeScript `place is possibly null` ในหน้าเพิ่มรูปจากรายละเอียดสถานที่
- [x] ใช้ชื่อสถานที่แบบปลอดภัยก่อนส่งเข้า upload queue
- [ ] รัน `npm.cmd run lint` บนเครื่องผู้ใช้
- [ ] รัน `npm.cmd run build` บนเครื่องผู้ใช้
- [ ] ทดสอบตั้งรูปหน้าปกและตรวจหน้า `/places`
- [ ] ยังไม่ Commit/Push จนกว่าจะทดสอบผ่าน


## Step 30 — Cover fallback + Plans UI
- [x] หน้า `/places` อ่านรูปหน้าปกจากข้อมูลรูปจริงและมี fallback ตามลำดับ
- [x] หน้า `/plans` ใช้ UI แนวเดียวกับหน้าสถานที่
- [x] แสดงรูป จุดหมาย วันที่ งบประมาณ หมายเหตุ จำนวนรูป และสถานะ
- [x] ใช้ข้อมูลจริงจาก Supabase

## Step 30.1 — React 19 lint fix
- [x] เลื่อน initial load ของหน้า `/places` ออกจาก effect body ด้วย timer callback
- [x] ยกวันที่อ้างอิงออกจาก render เพื่อไม่เรียก `Date.now()` ระหว่าง render
- [ ] รัน `npm.cmd run lint` บนเครื่องผู้ใช้
- [ ] รัน `npm.cmd run build` บนเครื่องผู้ใช้
- [ ] ตรวจรูปหน้าปกหน้า `/places`
- [ ] ตรวจหน้า `/plans` และตัวกรองสถานะ
- [ ] ยังไม่ Commit/Push จนกว่าจะทดสอบผ่าน

## Step 31 — Fast gallery + swipe lightbox
- [x] รูปในหน้ารายละเอียดกดเปิด Lightbox ได้จากตัวรูปโดยตรง
- [x] เอาข้อความ/ปุ่ม `ดูรูป` ออกจากรูปทุกใบ
- [x] ลดภาพตัวอย่างในแกลเลอรีเป็นประมาณ 480px
- [x] รูปแรกโหลดแบบ high priority และรูปถัดไป lazy load
- [x] Lightbox ใช้ Preview ความละเอียด 1600px เพื่อเปิดเร็วกว่า Original
- [x] โหลดรูปก่อนหน้าและรูปถัดไปล่วงหน้าใน Lightbox
- [x] รองรับปัดซ้าย–ขวาบนมือถือ
- [x] รองรับลูกศรซ้าย–ขวาและ Escape บน Desktop
- [x] ปิดการเลื่อนหน้าเบื้องหลังขณะเปิด Lightbox
- [x] คงระบบเลือกรูปหน้าปกผ่านปุ่มดาวบนรูป
- [ ] รัน `npm.cmd run lint`
- [ ] รัน `npm.cmd run build`
- [ ] ทดสอบรูป 1, 5 และ 10 รูปบนมือถือ
- [ ] ตรวจการปัดซ้าย–ขวาบน iOS และ Android
- [ ] ตรวจเวลาการโหลดรูปเก่าจาก Google Drive
- [ ] ยังไม่ Commit/Push จนกว่าจะทดสอบผ่าน

## Step 32 — Upload gauge + GAS 404 guard
- [x] เพิ่มเกจกราฟิกแสดงเปอร์เซ็นต์ใต้กรอบรูป
- [x] แสดงจำนวนรูปที่อัปโหลดสำเร็จเทียบกับรูปทั้งหมด เช่น `2 / 5 รูป`
- [x] ใช้เกจทั้งหน้าเพิ่มสถานที่และหน้าเพิ่มรูปในรายละเอียดสถานที่
- [x] ลดขนาด Payload ต่อรูปลงเพื่อให้ส่งผ่าน Next.js และ GAS เสถียรขึ้น
- [x] ตรวจ `GAS_WEB_APP_URL` ว่าต้องเป็น Web App URL ที่ลงท้าย `/exec`
- [x] เพิ่ม Health Check ของ GAS ก่อนอัปโหลดและ Cache ผลตรวจ 5 นาที
- [x] เพิ่ม Timeout เพื่อไม่ปล่อยให้ Request ค้างประมาณ 40 วินาทีโดยไม่จำเป็น
- [x] หาก GAS ตอบ HTML/404 ให้ลอง form-urlencoded สำรองหนึ่งครั้ง
- [x] แยก Error ที่ไม่ควร Retry เช่น URL ผิด, Unauthorized และ HTML 404
- [x] Retry เฉพาะ Error ชั่วคราวสูงสุด 3 ครั้ง
- [x] อัปเดตเวอร์ชันเป็น `0.3.4`
- [ ] Deploy `gas/Code.gs` เป็น New version และตรวจ URL `/exec`
- [ ] เปิด URL GAS แล้วต้องเห็น JSON service/version
- [ ] รัน `npm.cmd run lint`
- [ ] รัน `npm.cmd run build`
- [ ] ทดสอบอัปโหลด 1, 3, 5 และ 10 รูป
- [ ] ตรวจเกจเริ่ม 0%, เพิ่มตามจำนวนรูป และจบที่ 100%
- [ ] ยังไม่ Commit/Push จนกว่าจะทดสอบผ่าน


## Step 33 — ดาวคะแนนในหน้ารายละเอียด
- [x] แสดงดาว 5 ดวงในหน้ารายละเอียดสถานที่
- [x] เติมดาวสีเหลืองตามคะแนนจริงและรองรับคะแนนครึ่งดาว
- [x] แสดงคะแนนเป็นรูปแบบ `x.x / 5.0` ให้ตรงกับหน้าแรก
- [x] รัน `npm.cmd run lint` ผ่าน (มี warning เดิม 1 รายการ ไม่มี error)
- [x] รัน `npm.cmd run build` ผ่าน
- [ ] ตรวจหน้ารายละเอียดด้วยคะแนน 0, 0.5 และ 5.0 บนมือถือ
- [ ] ยังไม่ Commit/Push จนกว่าจะทดสอบผ่าน


## Step 34 — แก้แผนที่และค้นหาสถานที่ในหน้าวางแผน
- [x] เพิ่ม CDN สำรองสำหรับ Leaflet เมื่อแหล่งหลักโหลดไม่ได้
- [x] เพิ่มสถานะกำลังโหลด ข้อความผิดพลาด และปุ่มลองใหม่
- [x] ค้นหาทั้งชื่อดิบและชื่อพร้อมประเทศไทย
- [x] จัดอันดับชื่อที่ตรงกับคำค้นก่อนผลลัพธ์อื่น
- [x] แสดงคำแนะนำให้เพิ่มอำเภอ/จังหวัดเมื่อไม่พบผลลัพธ์
- [ ] ทดสอบคำว่า “เขากระโจม สวนผึ้ง ราชบุรี” บนมือถือจริง
- [ ] ยังไม่ Commit/Push จนกว่าจะทดสอบผ่าน
