import{NextRequest,NextResponse}from"next/server";export const runtime="nodejs";export async function GET(req:NextRequest,{params}:{params:Promise<{fileId:string}>}){const{fileId}=await params;const w=Math.min(2400,Math.max(160,Number(req.nextUrl.searchParams.get("w"))||960));for(const url of[`https://drive.google.com/thumbnail?id=${encodeURIComponent(fileId)}&sz=w${w}`,`https://lh3.googleusercontent.com/d/${encodeURIComponent(fileId)}=w${w}`,`https://drive.google.com/uc?export=view&id=${encodeURIComponent(fileId)}`]){try{const r=await fetch(url,{redirect:"follow",cache:"no-store"});const type=r.headers.get("content-type")||"";if(r.ok&&type.startsWith("image/"))return new NextResponse(await r.arrayBuffer(),{headers:{"Content-Type":type,"Cache-Control":"public,max-age=86400,s-maxage=604800,stale-while-revalidate=2592000"}})}catch{}}return NextResponse.redirect(new URL("/places/forest.svg",req.url))}
