"use client";import{useEffect,useState}from"react";import{useParams,useRouter}from"next/navigation";import{TravelHero}from"@/components/travel-hero";import{PlaceForm}from"@/components/place-form";import{getPlaceById,updatePlace,type TravelPlaceRow}from"@/lib/travel/repository";
export default function Edit(){const{id}=useParams<{id:string}>(),r=useRouter();const[p,setP]=useState<TravelPlaceRow|null>(null),[saving,setSaving]=useState(false);useEffect(()=>{getPlaceById(id).then(setP)},[id]);if(!p)return <p>กำลังโหลด…</p>;return <main><TravelHero title="แก้ไขสถานที่" subtitle={p.name} backHref={`/places/${id}`}/><section className="content"><PlaceForm initial={p} saving={saving} onSave={async data=>{setSaving(true);try{await updatePlace(id,data);r.push(`/places/${id}?updated=1`)}finally{setSaving(false)}}}/></section></main>}
